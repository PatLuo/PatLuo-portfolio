export interface BlogCard {
	title: string;
	description: string;
	currentSlug: string;
	image: any;
}

export interface BlogPostContent {
	title: string;
	currentSlug: string;
	image: any;
	content: string;
}
